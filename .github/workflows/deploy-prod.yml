name: Deploy to Production

on:
  pull_request:
    branches: [dev]
    types: [opened, synchronize, reopened]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🐳 Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🔧 Build Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/nestjs-app:production .

      - name: 🚀 Push Docker Image to Docker Hub
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/nestjs-app:production

      # - name: 🔐 SSH & Deploy to Server
      #   uses: appleboy/ssh-action@v1.0.0
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     script: |
      #       docker pull ${{ secrets.DOCKER_USERNAME }}/nestjs-app:latest
      #       docker stop nestjs-app || true
      #       docker rm nestjs-app || true
      #       docker run -d --name nestjs-app -p 3000:3000 ${{ secrets.DOCKER_USERNAME }}/nestjs-app:latest
